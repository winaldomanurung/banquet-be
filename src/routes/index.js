const user_router = require("./user-router");
const restaurant_router = require("./restaurant-router");
const reaction_router = require("./reaction-router");

module.exports = {
  user_router,
  restaurant_router,
  reaction_router,
};
